export enum RoutePath {
  GARAGE = '#/garage',
  WINNERS = '#/winners',
}

export enum ApiPath {
  GARAGE = '/garage',
  ENGINE = '/engine',
  WINNERS = '/winners',
}

export enum CarStatus {
  INIT = 'init',
  STARTED = 'started',
  DRIVING = 'driving',
  BROKEN = 'broken',
  FINISHED = 'finished',
}
